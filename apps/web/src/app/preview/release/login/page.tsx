"use client"

import { LoginCard } from "@/components/release"

export default function LoginPreview() {
  return (
    <LoginCard
      onLogin={() => {}}
      onGoogleLogin={() => {}}
      onRegisterClick={() => {}}
      onForgotPassword={() => {}}
    />
  )
}