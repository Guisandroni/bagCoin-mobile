"use client"

import { RegisterCard } from "@/components/release"

export default function RegisterPreview() {
  return (
    <RegisterCard
      onRegister={() => {}}
      onGoogleRegister={() => {}}
      onLoginClick={() => {}}
    />
  )
}