
# Production configuration
# with Traefik reverse proxy
#
# Usage:
#   1. Copy .env.prod.example to .env.prod and fill in values:
#      cp .env.prod.example .env.prod
#   2. Run: docker-compose -f docker-compose.prod.yml up -d
#
# Prerequisites:
#   1. Configure .env.prod with DOMAIN and ACME_EMAIL
#   2. Ensure ports 80 and 443 are available
#   3. Generate passwords: openssl rand -base64 32

services:
  traefik:
    image: traefik:v3.2
    container_name: bagcoin_api_traefik
    command:
      - "--api.dashboard=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=traefik-public"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      # Redirect HTTP to HTTPS
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      # Let's Encrypt
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL:-admin@example.com}"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
      # Access logs
      - "--accesslog=true"
      - "--accesslog.bufferingsize=100"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik_letsencrypt:/letsencrypt
    networks:
      - traefik-public
    labels:
      # Dashboard
      - "traefik.enable=true"
      - "traefik.http.routers.bagcoin_api-traefik.rule=Host(`traefik.${DOMAIN:-localhost}`)"
      - "traefik.http.routers.bagcoin_api-traefik.entrypoints=websecure"
      - "traefik.http.routers.bagcoin_api-traefik.tls.certresolver=letsencrypt"
      - "traefik.http.routers.bagcoin_api-traefik.service=api@internal"
      - "traefik.http.routers.bagcoin_api-traefik.middlewares=bagcoin_api-auth"
      - "traefik.http.middlewares.bagcoin_api-auth.basicauth.users=${TRAEFIK_DASHBOARD_AUTH:-admin:$$apr1$$xyz$$hash}"
      # Security headers middleware
      - "traefik.http.middlewares.bagcoin_api-security-headers.headers.stsSeconds=31536000"
      - "traefik.http.middlewares.bagcoin_api-security-headers.headers.stsIncludeSubdomains=true"
      - "traefik.http.middlewares.bagcoin_api-security-headers.headers.stsPreload=true"
      - "traefik.http.middlewares.bagcoin_api-security-headers.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.bagcoin_api-security-headers.headers.browserXssFilter=true"
      - "traefik.http.middlewares.bagcoin_api-security-headers.headers.referrerPolicy=strict-origin-when-cross-origin"
      - "traefik.http.middlewares.bagcoin_api-security-headers.headers.frameDeny=true"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

  app:
    build:
      context: ./apps/server
      dockerfile: Dockerfile
    container_name: bagcoin_api_backend
    volumes:
      - media_data:/app/media
    env_file:
      - .env.prod
      - ./apps/server/.env
    environment:
      - DEBUG=false
      - ENVIRONMENT=production
      - POSTGRES_HOST=db
      - REDIS_HOST=redis
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    networks:
      - traefik-public
      - backend-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bagcoin_api-api.rule=Host(`api.${DOMAIN:-localhost}`)"
      - "traefik.http.routers.bagcoin_api-api.entrypoints=websecure"
      - "traefik.http.routers.bagcoin_api-api.tls.certresolver=letsencrypt"
      - "traefik.http.routers.bagcoin_api-api.middlewares=bagcoin_api-security-headers@docker"
      - "traefik.http.services.bagcoin_api-api.loadbalancer.server.port=8000"
      - "traefik.docker.network=traefik-public"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M

  db:
    image: postgres:16-alpine
    container_name: bagcoin_api_db
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB:-bagcoin_api}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # NO external ports - only accessible within backend-internal network
    networks:
      - backend-internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M

  redis:
    image: redis:7-alpine
    container_name: bagcoin_api_redis
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    # NO external ports - only accessible within backend-internal network
    volumes:
      - redis_data:/data
    networks:
      - backend-internal
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

  celery_worker:
    image: bagcoin_api_backend:dev
    container_name: bagcoin_api_celery_worker
    volumes:
      - media_data:/app/media
    command: celery -A app.worker.celery_app worker --loglevel=warning --concurrency=4
    env_file:
      - .env.prod
      - ./apps/server/.env
    environment:
      - DEBUG=false
      - REDIS_HOST=redis
      - CELERY_BROKER_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
      - CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD}@redis:6379/0
    networks:
      - backend-internal
    depends_on:
      app:
        condition: service_started
      redis:
        condition: service_healthy
      db:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  celery_beat:
    image: bagcoin_api_backend:dev
    container_name: bagcoin_api_celery_beat
    command: celery -A app.worker.celery_app beat --loglevel=warning
    env_file:
      - .env.prod
      - ./apps/server/.env
    environment:
      - DEBUG=false
      - REDIS_HOST=redis
      - CELERY_BROKER_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
      - CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD}@redis:6379/0
    networks:
      - backend-internal
    depends_on:
      app:
        condition: service_started
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M

  flower:
    image: bagcoin_api_backend:dev
    container_name: bagcoin_api_flower
    command: celery -A app.worker.celery_app flower --port=5555
    env_file:
      - .env.prod
      - ./apps/server/.env
    environment:
      - DEBUG=false
      - REDIS_HOST=redis
      - CELERY_BROKER_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
      - CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD}@redis:6379/0
      - FLOWER_BASIC_AUTH=${FLOWER_USER:-admin}:${FLOWER_PASSWORD:?FLOWER_PASSWORD is required}
    networks:
      - traefik-public
      - backend-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bagcoin_api-flower.rule=Host(`flower.${DOMAIN:-localhost}`)"
      - "traefik.http.routers.bagcoin_api-flower.entrypoints=websecure"
      - "traefik.http.routers.bagcoin_api-flower.tls.certresolver=letsencrypt"
      - "traefik.http.services.bagcoin_api-flower.loadbalancer.server.port=5555"
      - "traefik.docker.network=traefik-public"
    depends_on:
      app:
        condition: service_started
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M

  web:
    build:
      context: ./apps/web
      target: prod
    container_name: bagcoin_web
    environment:
      - NEXT_PUBLIC_API_URL=https://api.${DOMAIN:-bagcoin.app}/api/v1
      - NEXT_PUBLIC_GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - NEXT_TELEMETRY_DISABLED=1
      - NODE_ENV=production
    networks:
      - traefik-public
      - backend-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bagcoin_web.rule=Host(`${DOMAIN:-bagcoin.app}`)"
      - "traefik.http.routers.bagcoin_web.entrypoints=websecure"
      - "traefik.http.routers.bagcoin_web.tls.certresolver=letsencrypt"
      - "traefik.http.routers.bagcoin_web.middlewares=bagcoin_web-security-headers@docker"
      - "traefik.http.services.bagcoin_web.loadbalancer.server.port=3000"
      - "traefik.http.middlewares.bagcoin_web-security-headers.headers.stsSeconds=31536000"
      - "traefik.http.middlewares.bagcoin_web-security-headers.headers.stsIncludeSubdomains=true"
      - "traefik.http.middlewares.bagcoin_web-security-headers.headers.stsPreload=true"
      - "traefik.http.middlewares.bagcoin_web-security-headers.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.bagcoin_web-security-headers.headers.browserXssFilter=true"
      - "traefik.http.middlewares.bagcoin_web-security-headers.headers.referrerPolicy=strict-origin-when-cross-origin"
      - "traefik.http.middlewares.bagcoin_web-security-headers.headers.frameDeny=true"
      - "traefik.docker.network=traefik-public"
    depends_on:
      - app
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M

networks:
  traefik-public:
    driver: bridge
  backend-internal:
    driver: bridge
    internal: true

volumes:
  media_data:
  traefik_letsencrypt:
  postgres_data:
  redis_data:
