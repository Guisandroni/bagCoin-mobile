name: save-transaction
description: Salva no banco
provider: opencode-go

tools:
  - name: insert_transaction

input_schema:
  transaction: object

output_schema:
  status: string

instructions: |
  Use a tool insert_transaction.
  Retorne sucesso.

temperature: 0
